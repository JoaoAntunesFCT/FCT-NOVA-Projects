#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Bem vindo! Por favor coloque números reais. ")
    int num=0;
	short int sucesso=0;
	while(!sucesso)
	{
		float x;
            printf("Introduza um valor para x:");
            scanf(\"%f", &x);

            fprintf(fPointer, "%f", amount);
            printf(\"\\n\");
            printf(\"x = %f\\ny = %f\\n\\n\", x, y);

		scanf(" %c", &c1);
		if(c1>=0 && )
		{
			printf("O programa vai encerrar devido a ter colocado uma letra.")
            exit(1);
		}
		if(c1==num)
		{
			sucesso = 1;
		}
		else
		{
			printf("Vá lá, escreve um 's': ");
		}
	}
	printf("Boa! Muito obrigado\n\n");
	printf("Agora escreva um número inteiro: ");
	scanf(" %hd", &num);
	printf("O número foi %d\n", num);
}
